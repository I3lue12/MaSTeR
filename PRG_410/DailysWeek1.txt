Monday
What are exceptions: Updated messages of what went wrong in code.

Tuesday
When should we write our own exceptions: When you have code that Visual studio does not have exceptions for

Wednesday	
What is the role of the finally block in exception handling? : Only needed if we still want to execute code despite failing connection to the database.

Thursday
What is the difference between an object and a reference?: References anchor objects and can manipulate the object. References can be initialized. Object is a valid piece of code.

Friday
Cite/name the base class for exceptions(sql or otherwise): System.exception

Saterday
What is nugget package manager in visual studio?: DLL's for people to use code that is already written

Sunday
How to automatically set breakpoint using exceptions, through Visual Studio.: When in the editor of visual studio on left hand side
